# Root requirements — everything needed to run the full pipeline
# (data generation -> warehouse -> analyses -> model -> API -> dashboard).
# Individual modules also carry their own requirements.txt (api/, dashboard/)
# for lean deploy images; this file is for local development.

pandas>=2.1
numpy>=1.26
pyarrow>=15.0
pyyaml>=6.0

duckdb>=1.0
dbt-core>=1.8
dbt-duckdb>=1.8

matplotlib>=3.8

scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
mlflow>=2.14
joblib>=1.3

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
pytest>=8.0

streamlit>=1.32
plotly>=5.20
requests>=2.31

ruff>=0.4
