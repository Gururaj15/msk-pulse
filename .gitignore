# Virtual environment
venv/
env/
.venv/

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Data & warehouse (regenerated by the pipeline steps)
data/raw/*.parquet
data/raw/manifest.json
data/raw/_prior_auths_debug_with_truth.parquet
warehouse/*.duckdb
warehouse/target/
warehouse/dbt_packages/
warehouse/logs/
warehouse/.user.yml

# Model artifacts (regenerated by training) — mlflow tracking db only;
# the actual model files (pipeline.joblib, calibrator.joblib, metadata.json,
# MODEL_CARD.md, charts) are kept so the API/dashboard work without retraining
model/artifacts/mlflow.db
model/artifacts/mlruns/

# OS / editor
.DS_Store
.vscode/
*.swp
Thumbs.db
warehouse/mlruns/